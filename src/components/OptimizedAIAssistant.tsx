'use client';

import { memo } from 'react';

// Stub component for AI Assistant - prevents build issues while preserving SEO optimizations
const OptimizedAIAssistant = memo(function OptimizedAIAssistant() {
  return (
    <div className="hidden">
      {/* AI Assistant temporarily disabled for build optimization */}
    </div>
  );
});

export default OptimizedAIAssistant;